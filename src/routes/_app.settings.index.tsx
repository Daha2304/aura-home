// Virtual leaf: /settings uses the settings layout's own index rendering.
// This file exists to make the /settings route stable in the generated
// route tree. Intentionally empty — the layout in `_app.settings.tsx`
// renders the index content when the pathname is exactly "/settings".
export {};
