export const glassBlur = {
  sm: "blur(14px) saturate(160%)",
  md: "blur(22px) saturate(170%)",
  lg: "blur(32px) saturate(190%)",
  xl: "blur(48px) saturate(200%)",
} as const;

export const glassRadius = {
  sm: "0.75rem",
  md: "1rem",
  lg: "1.25rem",
  xl: "1.75rem",
  "2xl": "2rem",
} as const;
