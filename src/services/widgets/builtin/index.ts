/**
 * Built-in Widget Descriptors.
 *
 * In diesem Schritt (Teil 5A) werden bewusst KEINE echten Smart-Home-Widgets
 * registriert. Diese Datei existiert als Plugin-Einstiegspunkt: neue Widget-
 * Pakete importieren `widgetRegistry` und rufen `register(defineWidget(...))`.
 *
 * Der bloße Import dieser Datei aus `bootstrap.ts` initialisiert die
 * Plugin-Kette, ohne dass UI-Code angefasst werden muss.
 */
export {}; // Side-effect-Datei ohne Registrierungen
